const { default: axios } = require("axios");
const { UserPair } = require("../models/index");
const baseUrl = "https://indodax.com";

class UserPairController {
  static async bookmark(req, res, next) {}
}

module.exports = UserPairController;
